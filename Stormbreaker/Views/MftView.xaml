<UserControl x:Class="Stormbreaker.Views.MftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Stormbreaker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <views:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <!-- Reusable Glass Card Panel Style -->
        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="17"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>

        <!-- Glass Chip ToggleButton Style -->
        <Style x:Key="ChipToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="#0DFFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1AFFFFFF"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource GlassBorderLightBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#26ef4444"/> <!-- Transparent red -->
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentRedBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentRedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Page title -->
            <RowDefinition Height="Auto"/> <!-- Stats Grid -->
            <RowDefinition Height="*"/>    <!-- Logs list container -->
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <ContentControl Content="{StaticResource IconHardDrive}" Style="{StaticResource DfirIcon}" Foreground="{StaticResource AccentCyanBrush}" Width="20" Height="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Txt[MftHeader]}" FontSize="19" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="{Binding Txt[MftSub]}" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            
            <Border Background="#1F00d4ff" BorderBrush="#5100d4ff" BorderThickness="1" CornerRadius="12" Padding="12,4" HorizontalAlignment="Right" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding FilteredMft.Count, FallbackValue=0}" Foreground="{StaticResource AccentCyanBrush}" FontWeight="Bold"/>
                    <TextBlock Text=" / " Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBlock Text="{Binding Mft.Count, FallbackValue=0}" Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBlock Text="{Binding Txt[MftCountSuffix]}" Foreground="{StaticResource TextMutedBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Stats Grid (4 Columns) -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- MFT Records -->
            <Border Style="{StaticResource GlassPanel}" Margin="0,0,7,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <TextBlock Text="{Binding Txt[KpiMft]}" FontSize="10.5" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <Border Width="30" Height="30" CornerRadius="8" Background="#1F8b5cf6" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <ContentControl Content="{StaticResource IconHardDrive}" Style="{StaticResource DfirIcon}" Foreground="{StaticResource AccentPurpleBrush}" Width="16" Height="16"/>
                        </Border>
                    </Grid>
                    <TextBlock Grid.Row="1" Text="{Binding MftTotalCount, FallbackValue=10}" FontSize="25" FontWeight="Bold" Foreground="White" Margin="0,6,0,0"/>
                    <TextBlock Grid.Row="2" Text="{Binding Txt[MftIndexed]}" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
                </Grid>
            </Border>

            <!-- Timestomp Flags -->
            <Border Style="{StaticResource GlassPanel}" Margin="7,0,7,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <TextBlock Text="{Binding Txt[MftKpiTimestomp]}" FontSize="10.5" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <Border Width="30" Height="30" CornerRadius="8" Background="#1Fef4444" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <ContentControl Content="{StaticResource IconAlert}" Style="{StaticResource DfirIcon}" Foreground="{StaticResource AccentRedBrush}" Width="16" Height="16"/>
                        </Border>
                    </Grid>
                    <TextBlock Grid.Row="1" Text="{Binding MftTimestompCount, FallbackValue=3}" FontSize="25" FontWeight="Bold" Foreground="White" Margin="0,6,0,0"/>
                    <TextBlock Grid.Row="2" Text="{Binding Txt[MftSiBackdated]}" FontSize="11" FontWeight="Medium" Foreground="{StaticResource AccentRedBrush}" Margin="0,4,0,0"/>
                </Grid>
            </Border>

            <!-- ADS / MOTW -->
            <Border Style="{StaticResource GlassPanel}" Margin="7,0,7,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <TextBlock Text="ADS / MOTW" FontSize="10.5" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <Border Width="30" Height="30" CornerRadius="8" Background="#1F00d4ff" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <ContentControl Content="{StaticResource IconEye}" Style="{StaticResource DfirIcon}" Foreground="{StaticResource AccentCyanBrush}" Width="16" Height="16"/>
                        </Border>
                    </Grid>
                    <TextBlock Grid.Row="1" Text="{Binding MftAdsCount, FallbackValue=1}" FontSize="25" FontWeight="Bold" Foreground="White" Margin="0,6,0,0"/>
                    <TextBlock Grid.Row="2" Text="Zone.Identifier = 3" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
                </Grid>
            </Border>

            <!-- Zeroed Files -->
            <Border Style="{StaticResource GlassPanel}" Margin="7,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <TextBlock Text="{Binding Txt[MftZeroedFiles]}" FontSize="10.5" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <Border Width="30" Height="30" CornerRadius="8" Background="#1Ff59e0b" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <ContentControl Content="{StaticResource IconTrash}" Style="{StaticResource DfirIcon}" Foreground="{StaticResource AccentOrangeBrush}" Width="16" Height="16"/>
                        </Border>
                    </Grid>
                    <TextBlock Grid.Row="1" Text="{Binding MftZeroedCount, FallbackValue=2}" FontSize="25" FontWeight="Bold" Foreground="White" Margin="0,6,0,0"/>
                    <TextBlock Grid.Row="2" Text="{Binding Txt[MftDataDestruction]}" FontSize="11" FontWeight="Medium" Foreground="{StaticResource AccentRedBrush}" Margin="0,4,0,0"/>
                </Grid>
            </Border>
        </UniformGrid>

        <!-- Table Container -->
        <Border Grid.Row="2" Style="{StaticResource GlassPanel}" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Toolbar (Search + Toggle) -->
                    <RowDefinition Height="Auto"/> <!-- Table Headers -->
                    <RowDefinition Height="*"/>    <!-- Scrollable content rows -->
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Grid Grid.Row="0" Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <Border Grid.Column="0" BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="1" Background="#40000000" CornerRadius="8" Padding="12,6" HorizontalAlignment="Stretch" Margin="0,0,14,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ContentControl Content="{StaticResource IconSearch}" Style="{StaticResource DfirIcon}" Width="15" Height="15" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="13" VerticalAlignment="Center" CaretBrush="{StaticResource AccentCyanBrush}"
                                     Text="{Binding MftSearchText, UpdateSourceTrigger=PropertyChanged}"
                                     d:Text="svchost"/>
                        </Grid>
                    </Border>

                    <!-- Stomp filter ToggleButton -->
                    <ToggleButton Grid.Column="1" Style="{StaticResource ChipToggleButton}" IsChecked="{Binding MftStompOnly, Mode=TwoWay}">
                        <StackPanel Orientation="Horizontal">
                            <ContentControl Content="{StaticResource IconAlert}" Style="{StaticResource DfirIcon}" Width="13" Height="13" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Txt[MftStompOnly]}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ToggleButton>
                </Grid>

                <!-- Grid Table Headers -->
                <Border Grid.Row="1" Background="#D6101628" BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="0,0,0,1" Padding="8,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70"/>  <!-- Rec -->
                            <ColumnDefinition Width="2.5*"/> <!-- Path -->
                            <ColumnDefinition Width="75"/>  <!-- Size -->
                            <ColumnDefinition Width="150"/> <!-- $SI created -->
                            <ColumnDefinition Width="150"/> <!-- $FN created -->
                            <ColumnDefinition Width="1.5*"/>   <!-- Assessment -->
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding Txt[ColRec]}" FontWeight="Bold" FontSize="10" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding Txt[ColFilePath]}" FontWeight="Bold" FontSize="10" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding Txt[ColSize]}" FontWeight="Bold" FontSize="10" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="{Binding Txt[ColSiCreated]}" FontWeight="Bold" FontSize="10" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="{Binding Txt[ColFnCreated]}" FontWeight="Bold" FontSize="10" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="5" Text="{Binding Txt[ColAssessment]}" FontWeight="Bold" FontSize="10" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Data Rows -->
                <ListBox Grid.Row="2" Background="Transparent" BorderThickness="0" Padding="0" Focusable="False"
                         HorizontalContentAlignment="Stretch" VerticalContentAlignment="Top"
                         ItemsSource="{Binding FilteredMft, Mode=OneWay}">
                    
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Row outer border -->
                            <Border x:Name="RowBorder" BorderBrush="#0AFFFFFF" BorderThickness="0,0,0,1" Padding="8,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>  <!-- Rec -->
                                        <ColumnDefinition Width="2.5*"/> <!-- Path -->
                                        <ColumnDefinition Width="75"/>  <!-- Size -->
                                        <ColumnDefinition Width="150"/> <!-- $SI created -->
                                        <ColumnDefinition Width="150"/> <!-- $FN created -->
                                        <ColumnDefinition Width="1.5*"/>   <!-- Assessment -->
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Rec}" Foreground="White" FontFamily="{StaticResource FontMono}" FontSize="12" VerticalAlignment="Top"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Path}" Foreground="White" FontFamily="{StaticResource FontMono}" FontSize="12" VerticalAlignment="Top" TextWrapping="Wrap" Margin="0,0,10,0"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Size}" Foreground="{StaticResource TextMutedBrush}" FontFamily="{StaticResource FontMono}" FontSize="12" VerticalAlignment="Top"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Si}" Foreground="{StaticResource TextSecondaryBrush}" FontFamily="{StaticResource FontMono}" FontSize="12" VerticalAlignment="Top"/>
                                    <TextBlock Grid.Column="4" Text="{Binding Fn}" Foreground="{StaticResource TextSecondaryBrush}" FontFamily="{StaticResource FontMono}" FontSize="12" VerticalAlignment="Top"/>
                                    
                                    <!-- Assessment cell (toggles badges based on Flag) -->
                                    <StackPanel Grid.Column="5" VerticalAlignment="Top">
                                        <!-- Stomped badge -->
                                        <Border x:Name="StompedBadge" CornerRadius="5" Padding="7,2" HorizontalAlignment="Left" Background="#26ef4444" Visibility="Collapsed">
                                            <TextBlock Text="timestomp" FontSize="9.5" FontWeight="Bold" Foreground="{StaticResource AccentRedBrush}"/>
                                        </Border>
                                        <!-- Consistent badge -->
                                        <Border x:Name="ConsistentBadge" CornerRadius="5" Padding="7,2" HorizontalAlignment="Left" Background="#2622c55e">
                                            <TextBlock Text="consistent" FontSize="9.5" FontWeight="Bold" Foreground="{StaticResource AccentGreenBrush}"/>
                                        </Border>
                                        
                                        <!-- Assessment Note -->
                                        <TextBlock Text="{Binding Note}" FontSize="10.5" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- Triggers for row assessment styles -->
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding Flag}" Value="True">
                                    <Setter TargetName="RowBorder" Property="Background" Value="#0Fef4444"/> <!-- 6% transparent red backplate -->
                                    <Setter TargetName="StompedBadge" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="ConsistentBadge" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
