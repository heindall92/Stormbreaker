<UserControl x:Class="Stormbreaker.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Stormbreaker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <views:SeverityToBrushConverter x:Key="SevToBrush"/>
        <views:BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Reusable Glass Card Panel Style -->
        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="17"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>

        <!-- Reusable Chip Filter Button Style -->
        <Style x:Key="ChipButton" TargetType="Button">
            <Setter Property="Background" Value="#0DFFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassBorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1AFFFFFF"/>
                    <Setter Property="BorderBrush" Value="{StaticResource GlassBorderLightBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Timeline Row Hover Style -->
        <Style x:Key="TimelineRowStyle" TargetType="Border">
            <Setter Property="Background" Value="#06FFFFFF"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0C00d4ff"/> <!-- Subtle cyan highlight -->
                    <Setter Property="BorderBrush" Value="#2600D4FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Page title -->
            <RowDefinition Height="*"/>    <!-- Timeline list container -->
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <ContentControl Content="{StaticResource IconClock}" Style="{StaticResource DfirIcon}" Foreground="{StaticResource AccentCyanBrush}" Width="20" Height="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Txt[TimelineHeader]}" FontSize="19" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="{Binding Txt[TimelineSub]}" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            
            <Border Background="#1F00d4ff" BorderBrush="#5100d4ff" BorderThickness="1" CornerRadius="12" Padding="12,4" HorizontalAlignment="Right" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding TimelineEvents.Count, FallbackValue=0}" Foreground="{StaticResource AccentCyanBrush}" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Txt[EventsCountSuffix]}" Foreground="{StaticResource TextMutedBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Timeline Container -->
        <Border Grid.Row="1" Style="{StaticResource GlassPanel}" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Toolbar (Chips) -->
                    <RowDefinition Height="*"/>    <!-- Scrollable timeline -->
                </Grid.RowDefinitions>

                <!-- Toolbar (Filter Chips) -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
                    <!-- All -->
                    <Button Command="{Binding SetTimelineFilterCommand}" CommandParameter="all">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ChipButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TimelineFilterLvl}" Value="all">
                                        <Setter Property="Background" Value="#2600d4ff"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource AccentCyanBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentCyanBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="{Binding Txt[TimelineAllSources]}"/>
                    </Button>
                    
                    <!-- Critical -->
                    <Button Command="{Binding SetTimelineFilterCommand}" CommandParameter="critical">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ChipButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TimelineFilterLvl}" Value="critical">
                                        <Setter Property="Background" Value="#26ef4444"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource AccentRedBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentRedBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="{Binding Txt[SeverityCritical]}"/>
                    </Button>
                    
                    <!-- Warning -->
                    <Button Command="{Binding SetTimelineFilterCommand}" CommandParameter="warning">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ChipButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TimelineFilterLvl}" Value="warning">
                                        <Setter Property="Background" Value="#26f59e0b"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource AccentOrangeBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentOrangeBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="{Binding Txt[SeverityWarning]}"/>
                    </Button>
                    
                    <!-- Info -->
                    <Button Command="{Binding SetTimelineFilterCommand}" CommandParameter="info">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ChipButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TimelineFilterLvl}" Value="info">
                                        <Setter Property="Background" Value="#263b82f6"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentBlueBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="{Binding Txt[SeverityInfo]}"/>
                    </Button>

                    <!-- Success -->
                    <Button Command="{Binding SetTimelineFilterCommand}" CommandParameter="success">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ChipButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TimelineFilterLvl}" Value="success">
                                        <Setter Property="Background" Value="#2622c55e"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource AccentGreenBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentGreenBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="{Binding Txt[SeverityNotable]}"/>
                    </Button>
                </StackPanel>

                <!-- Chronological Timeline List -->
                <ListBox Grid.Row="1" Background="Transparent" BorderThickness="0" Padding="0" Focusable="False"
                         HorizontalContentAlignment="Stretch" VerticalContentAlignment="Top"
                         ItemsSource="{Binding TimelineEvents, Mode=OneWay}">
                    
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Row outer border -->
                            <Border Style="{StaticResource TimelineRowStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/> <!-- Dot -->
                                        <ColumnDefinition Width="*"/>    <!-- Text content -->
                                        <ColumnDefinition Width="Auto"/> <!-- Time -->
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Severity Dot -->
                                    <Border Grid.Column="0" Width="10" Height="10" CornerRadius="5" Margin="0,4,14,0" VerticalAlignment="Top"
                                            Background="{Binding Lvl, Converter={StaticResource SevToBrush}}">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="8" ShadowDepth="0" Color="White" Opacity="0.45"/>
                                        </Border.Effect>
                                    </Border>

                                    <!-- Content details -->
                                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Src}" FontSize="12.5" FontWeight="Bold" Foreground="White"/>
                                            <TextBlock Text=" · EID " FontSize="11.5" Foreground="{StaticResource TextMutedBrush}"/>
                                            <TextBlock Text="{Binding Id}" FontSize="11.5" Foreground="{StaticResource TextMutedBrush}" FontWeight="SemiBold"/>
                                            
                                            <!-- MITRE Badges -->
                                            <ItemsControl ItemsSource="{Binding Mitre}" Margin="8,0,0,0" Focusable="False">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel Orientation="Horizontal"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="#1F8b5cf6" BorderBrush="#518b5cf6" BorderThickness="1" CornerRadius="5" Padding="5,0.5" Margin="2,0">
                                                            <TextBlock Text="{Binding}" Foreground="{StaticResource AccentPurpleBrush}" FontFamily="{StaticResource FontMono}" FontSize="9" FontWeight="SemiBold"/>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                        
                                        <!-- Message string -->
                                        <TextBlock Text="{Binding Msg}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>

                                    <!-- Time column -->
                                    <TextBlock Grid.Column="2" Text="{Binding T}" FontSize="11" FontFamily="{StaticResource FontMono}" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Top"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
